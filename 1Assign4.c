#include<stdio.h>
int main()
{
    float r,a;
    printf("Entre the radius:");
    scanf("%f",&r);
    a=3.14*r*r;
    printf("Area of circle is %f having the radius %f",a,r);
    return 0;
}